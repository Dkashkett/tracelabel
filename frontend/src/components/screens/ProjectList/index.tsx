// Placeholder — filled in by F2-PROJECTS (docs/refactor-plan.md §4).
// Cards for each project, a "New project" dialog, and a "Start from demo data"
// button that routes through the normal import path.
export default function ProjectList() {
  return (
    <div className="p-8">
      <h1 className="text-lg font-semibold">Projects</h1>
      <p className="mt-2 text-sm text-ink-muted">
        Placeholder screen. F2-PROJECTS fills this in with project cards and a
        new-project dialog.
      </p>
    </div>
  );
}
