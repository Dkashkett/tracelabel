// Placeholder — filled in by F2-SETTINGS (docs/refactor-plan.md §4).
// Annotator name, default LLM model, theme. No API key field — env vars only.
export default function Settings() {
  return (
    <div className="p-8">
      <h1 className="text-lg font-semibold">Settings</h1>
      <p className="mt-2 text-sm text-ink-muted">
        Placeholder screen. F2-SETTINGS fills this in with annotator/model/theme
        fields.
      </p>
    </div>
  );
}
